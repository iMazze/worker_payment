controllers:
    resource: '../../Controller/'
    type: annotation
    prefix: /{_locale}
    requirements:
        _locale: '%app_locales%'
    defaults:
        _locale: '%locale%'

emptydescriptionchecker.api:
    resource: '../var/plugins/worker_payment/API/'
    type: rest
    prefix: /api
