controllers:
    resource: '../../Controller/'
    type: annotation
    prefix: /{_locale}
    requirements:
        _locale: '%app_locales%'
    defaults:
        _locale: '%locale%'

emptydescriptionchecker.api:
    resource: '../var/plugins/EmptyDescriptionCheckerBundle/API/'
    type: rest
    prefix: /api
